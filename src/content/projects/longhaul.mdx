---
slug: longhaul
title: longhaul
tagline: >-
  Early-adopter MCP 2026-07-28 release-candidate server in Rust — stateless
  core, the Tasks extension, and statelessness proven by round-robining one
  client across two server instances.
repo: https://github.com/slatino-dev/longhaul
language: Rust
status: active
order: 2
summary: >-
  An implementation of the MCP 2026-07-28 release candidate, built while the
  spec is still wet: stateless core (per-request _meta clientInfo,
  MCP-Protocol-Version / Mcp-Method / Mcp-Name headers, server/discover in
  place of the removed initialize handshake), the Tasks extension (tools/call
  returns a task handle driven by tasks/get, tasks/update, tasks/cancel),
  InputRequired multi-round-trip, JSON Schema 2020-12 tool schemas. axum
  server, a TaskStore trait with memory and SQLite backends, Mcp-Method
  routing middleware, a real long-running indexer example, and a conformance
  runner. The signature test round-robins one client's task lifecycle across
  two server instances that share only SQLite.
topics:
  - MCP
  - protocol implementation
  - stateless servers
  - axum
  - conformance testing
tests: 67
---

**The MCP 2026-07-28 release candidate deletes the initialize handshake and assumes your server keeps no session — longhaul is a working answer to "fine: how?", written while the spec is still moving.**

This is an implementation log more than a product page. I built a server
against the release candidate as it landed, partly to have one, mostly because
reading a spec and implementing a spec are different levels of understanding
and the second one is the only one that sticks.

## log 01 — the handshake is gone

The RC removes `initialize`. There is no session to set up because there is no
session: `server/discover` answers capability questions statelessly, and
everything identity-shaped now rides on each request — `clientInfo` arrives
per-request in `_meta`, and three headers carry the rest.

<TerminalCapture title="one request carries everything" caption="no handshake, no session — each request is self-describing">

```text
POST /
MCP-Protocol-Version: 2026-07-28
Mcp-Method: tools/call
Mcp-Name: indexer

{ "jsonrpc": "2.0", "method": "tools/call",
  "params": { ..., "_meta": { "clientInfo": { ... } } } }
```

</TerminalCapture>

The `Mcp-Method` header is a gift for routing: axum middleware dispatches on
it before the body is parsed, so the JSON-RPC layer stays thin. Tool schemas
are JSON Schema 2020-12. One error code moved — what was `-32002` is now
`-32602` — which is a small diff in the spec and a grep through every test
fixture in practice.

## log 02 — long-running work becomes Tasks

The Tasks extension restructures `tools/call`: instead of blocking on slow
work, the call returns a task handle, and the client drives the lifecycle with
`tasks/get`, `tasks/update`, and `tasks/cancel`. `tasks/list` is gone from the
RC. A task can pause in `InputRequired`, ask the client a question, and resume
when the answer comes back via `tasks/update` — a real multi-round-trip, not a
callback hack.

The repo's example server makes this concrete: an indexer whose
`index_directory` task walks a real directory tree with streamed progress,
honors cancellation mid-walk, and exercises one full `InputRequired` round
trip. A conformance runner drives the whole protocol surface from the outside.

<DecisionNote title="a hand-written typed subset over codegen">

Generating Rust types from the spec schema looked like the responsible choice
right up until the spec moved. Against a release candidate, codegen
regenerates a large surface on every draft and the diff stops meaning
anything; churn buries the actual semantic changes. The hand-written subset
covers exactly what the server implements, each type sits next to a comment
naming its spec section, and when the RC renames something the compiler hands
me a worklist.

The cost is honest: coverage is partial by construction, and new protocol
surface means typing it out by hand. For a moving spec I would make the same
trade again; for a frozen 1.0, codegen wins and I would switch.

</DecisionNote>

## log 03 — proving statelessness instead of asserting it

Every server claims to be stateless. The signature test makes the claim
falsifiable: spin up two longhaul instances that share nothing but a SQLite
file, then round-robin one client's entire task lifecycle across them —
create the task on instance A, poll it on B, answer its `InputRequired`
question on A, cancel a sibling on B. If any state hides in process memory,
the lifecycle breaks and the test fails.

<ArchDiagram path="docs/statelessness.svg" caption="the signature test: one client, two instances, alternating requests — SQLite is the only shared state">
  <svg viewBox="0 0 600 320" width="600" height="320" role="presentation">
    <defs>
      <marker id="lh-g" viewBox="0 0 8 8" refX="7" refY="4" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path class="arrow-accent" d="M0 0L8 4L0 8z" />
      </marker>
    </defs>
    <rect class="box" x="10" y="130" width="120" height="68" rx="3" />
    <text class="lbl" x="70" y="158" text-anchor="middle">client</text>
    <text class="sub" x="70" y="176" text-anchor="middle">one task lifecycle</text>
    <rect class="box" x="210" y="20" width="250" height="96" rx="3" />
    <text class="lbl" x="335" y="44" text-anchor="middle">longhaul — instance A</text>
    <text class="sub" x="335" y="62" text-anchor="middle">axum · Mcp-Method router</text>
    <text class="sub" x="335" y="77" text-anchor="middle">tools/call → task handle</text>
    <text class="sub" x="335" y="92" text-anchor="middle">tasks/get · update · cancel</text>
    <rect class="box" x="210" y="150" width="250" height="96" rx="3" />
    <text class="lbl" x="335" y="174" text-anchor="middle">longhaul — instance B</text>
    <text class="sub" x="335" y="192" text-anchor="middle">same binary, separate process</text>
    <text class="sub" x="335" y="207" text-anchor="middle">no session · no handshake</text>
    <text class="sub" x="335" y="222" text-anchor="middle">per-request _meta clientInfo</text>
    <rect class="box box-accent" x="495" y="92" width="95" height="86" rx="3" />
    <text class="lbl" x="542" y="120" text-anchor="middle">SQLite</text>
    <text class="sub" x="542" y="138" text-anchor="middle">TaskStore</text>
    <text class="sub" x="542" y="153" text-anchor="middle">only shared state</text>
    <polyline class="edge edge-accent" points="130,150 170,150 170,68 206,68" marker-end="url(#lh-g)" />
    <polyline class="edge edge-accent" points="130,178 170,178 170,198 206,198" marker-end="url(#lh-g)" />
    <text class="note" x="160" y="132" text-anchor="end">round-robin</text>
    <polyline class="edge edge-accent" points="460,68 478,68 478,116 491,116" marker-end="url(#lh-g)" />
    <polyline class="edge edge-accent" points="460,198 478,198 478,150 491,150" marker-end="url(#lh-g)" />
    <text class="note" x="300" y="288" text-anchor="middle">create on A → poll on B → answer InputRequired on A → cancel on B</text>
  </svg>
</ArchDiagram>

Storage sits behind a `TaskStore` trait — `MemoryStore` keeps unit tests fast,
`SqliteStore` carries the proof. 67 tests, `clippy -D warnings` clean.

<StatGrid cols={4}>
  <Metric label="tests" value="67" detail="incl. the round-robin proof" />
  <Metric label="task stores" value="2" detail="memory · SQLite" />
  <Metric label="spec" value="2026-07-28" detail="release candidate" />
  <Metric label="lint" value="clippy -D" detail="zero warnings" />
</StatGrid>

## log 04 — what conformance will say

<BenchTable
  path="conformance/matrix"
  emptyNote="The conformance runner exists and runs; published results don't, yet. What lands here: per-method conformance for this server against the RC surface, plus task-lifecycle timings across the memory and SQLite stores. Until those runs are written up, this table stays empty rather than approximate."
/>

<DeadEnd title="implementing surface the spec hadn't settled">

I implemented `tasks/list` because an earlier draft had it. The RC removed it,
and the deletion cost more than the feature: handler, store queries, pagination
semantics, tests, all gone. The early-adopter tax is real, and the lesson is
specific — build the minimum the spec forces, and resist completing surface
that has not survived a draft cycle. I would also start the conformance runner
on day one rather than after the core; it caught spec drift that unit tests,
which encode my own reading of the spec, were structurally unable to see.

</DeadEnd>
